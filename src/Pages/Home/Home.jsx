import React from 'react'
import styles from './Home.module.css'
import Form from '../../Components/Form/Form'

/**
 * This is a functional component that returns a form.
 * @returns A component called `Form` is being returned.
 */

const Home = () => {
  return <Form />
}

export default Home
